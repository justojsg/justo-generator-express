
router.{{scope.method}}("/{{scope.path}}", function(req, res) {
  const app = req.app;
  {{#if (eq scope.method "post")}}
  const params = req.body;
  {{else}}
  const params = req.params;
  {{/if}}

  {{#if scope.view}}
  res.render("{{scope.view}}", {});
  {{else}}
  //res.send(...);
  //res.json(...);
  //res.sendFile(...);
  {{/if}}
});
